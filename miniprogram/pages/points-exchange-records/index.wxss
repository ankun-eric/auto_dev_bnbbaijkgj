.exchange-records {
  min-height: 100vh;
  background: #f5f7f5;
  padding-bottom: 40rpx;
}

.header {
  background: #C8E6C9;
  padding: 28rpx 32rpx;
  display: flex;
  flex-direction: column;
}
.header .title { color: #1B5E20; font-size: 28rpx; font-weight: bold; }
.header .tip { color: #2E7D32; font-size: 22rpx; margin-top: 6rpx; }

.loading-tip, .empty-tip, .no-more {
  text-align: center;
  color: #999;
  font-size: 24rpx;
  padding: 60rpx 0;
}

.record-list { padding: 20rpx 24rpx; display: flex; flex-direction: column; gap: 20rpx; }

.record-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  display: flex;
  gap: 20rpx;
}

.record-left {
  width: 96rpx;
  height: 96rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.record-img { width: 80rpx; height: 80rpx; }
.record-icon { font-size: 48rpx; }

.record-body { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 6rpx; }

.tag-row { display: flex; gap: 10rpx; }
.tag {
  font-size: 20rpx;
  padding: 2rpx 12rpx;
  border-radius: 16rpx;
  line-height: 1.6;
}
.tag.outline {
  background: #fff;
  border: 1rpx solid;
}

.goods-name {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.time-row, .expire-row { font-size: 22rpx; color: #999; }
.expire-row { color: #fa8c16; }

.bottom-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 10rpx;
}
.points { color: #B8860B; font-weight: 600; font-size: 26rpx; }

.btn {
  padding: 8rpx 22rpx;
  border-radius: 24rpx;
  font-size: 22rpx;
  line-height: 1.4;
}
.btn-primary {
  background: linear-gradient(135deg, #52c41a, #13c2c2);
  color: #fff;
}
.btn-outline {
  border: 1rpx solid #fa8c16;
  color: #fa8c16;
  background: rgba(250,140,22,0.08);
}
.btn-outline.purple {
  border-color: #722ed1;
  color: #722ed1;
  background: rgba(114,46,209,0.08);
}

/* OPT-4：优惠券双按钮组 */
.btn-group {
  display: flex;
  gap: 12rpx;
}
