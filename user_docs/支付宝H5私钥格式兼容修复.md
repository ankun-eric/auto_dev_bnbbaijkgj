# 支付宝 H5「应用私钥」格式兼容修复 · 用户体验使用手册

> 文档版本：2026-05-05  
> 修复编号：cursor_prompt_349  
> 修复主题：彻底解决「应用私钥格式不被支持」阻断性 Bug

---

## 访问链接

以下是当前项目的体验链接，点击即可打开：

> ⚠️ 所有链接均使用宿主机 Nginx 代理端口（80/443），请勿使用 Docker 容器内部端口。

| 服务 | 链接 | 说明 |
|------|------|------|
| 管理后台（admin-web） | [https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/](https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/) | 登录后进入「支付配置」页面体验本次修复 |
| H5 用户端 | [https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/](https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/) | 终端用户的下单与支付入口 |

---

## 功能简介

本次修复**彻底解决**了运营人员在管理后台「支付配置 → 支付宝 H5」页面，
粘贴支付宝官方密钥工具产出的「应用私钥」后，点击「测试」**一定会报
"应用私钥格式不被支持"** 的阻断性 Bug。

### 修复前的痛点

- 支付宝官方密钥工具会**同时生成两份私钥文件**：
  - `应用私钥PKCS8.txt`（PKCS#8 格式）
  - `应用私钥RSA2048.txt`（PKCS#1 格式）
- 老逻辑只接受 `PKCS8.txt` 内容，运营人员若误粘 `RSA2048.txt` 内容，
  系统会直接报错「应用私钥格式不被支持，请使用 PKCS8.txt 不是 RSA2048.txt」。
- 即使粘对了 `PKCS8.txt`，**带上 `-----BEGIN PRIVATE KEY-----` 头尾**也会被一刀切短路报错。
- 运营人员在两份相似的 `.txt` 文件之间反复确认，每次新增商户、轮换密钥都会再踩坑。

### 修复后的能力

后端 `normalize_rsa_private_key` **自动识别并归一化**支付宝官方工具产出的
**任何一种**合法私钥形态，下游 SDK 不需要任何调整：

| # | 形态 | 来源 | 是否支持 |
|---|------|------|:---:|
| 1 | PKCS#8 中间 Base64（无头尾） | `应用私钥PKCS8.txt` 直接复制 | ✅ |
| 2 | PKCS#8 完整 PEM（含 `BEGIN PRIVATE KEY`） | 用户复制时一起带上头尾 | ✅ 自动剥离 |
| 3 | **PKCS#1 中间 Base64（无头尾）** | **`应用私钥RSA2048.txt` 直接复制** | ✅ 本次新增 |
| 4 | PKCS#1 完整 PEM（含 `BEGIN RSA PRIVATE KEY`） | 部分老工具 / openssl 输出 | ✅ 本次新增 |
| 5 | 任一以上形态 + 不可见字符（BOM / 零宽空格 / 全角空格 / 换行 / Tab） | 浏览器 / 微信复制 | ✅ 自动清洗 |
| 6 | 误粘公钥 | — | ❌ 给出"您粘贴的是公钥"针对性报错 |

入库前**统一转换为 PKCS#8 PEM**，加密落库，下游 `alipay_service` 完全不需要调整。

### 报错文案分支化

修复后的报错文案不再"一刀切"，而是按错因给出针对性建议：

- **完全无法识别**：「应用私钥无法识别。请确认您粘贴的是支付宝密钥工具生成的「应用私钥」内容（PKCS8.txt 或 RSA2048.txt 任一即可）。」
- **疑似公钥**：「您粘贴的内容看起来是「公钥」而不是「私钥」。请检查文件是否为应用私钥（PKCS8.txt 或 RSA2048.txt）。」
- **内容被截断**：「私钥内容看起来不完整，请重新完整复制粘贴。」
- **网络不通**：「网络不通：无法连接支付宝网关，请检查出网/防火墙」

### 前端层友好引导

管理后台「应用私钥（PEM）」输入框：

- **placeholder** 改为：「把密钥工具生成的「应用私钥」内容直接粘贴进来即可（PKCS8.txt 或 RSA2048.txt 都行）」
- 输入框下方新增**永久性帮助提示**：
  > 💡 把支付宝密钥工具生成的「应用私钥」文件内容直接粘贴进来即可：
  > PKCS8.txt 或 RSA2048.txt 都支持，含/不含 BEGIN/END 头尾都行。

---

## 使用说明

### 场景一：首次配置支付宝 H5 通道

1. 用浏览器打开管理后台：
   [https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/](https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/)
2. 使用管理员账号登录，左侧菜单进入「**支付配置**」。
3. 找到「**支付宝 H5 支付**」一行，点击右侧「**配置**」按钮，弹出右侧抽屉。
4. 填写以下字段：
   - **应用 AppID**：支付宝开放平台分配的 16 位数字
   - **接入模式**：选择「公钥模式」或「证书模式」（按贵公司支付宝商户后台配置情况选择）
   - **应用私钥（PEM）**：⭐ 重点字段
   - **支付宝公钥** / **应用公钥证书** 等：按所选接入模式填写对应字段
5. 在「应用私钥（PEM）」输入框，**直接打开**支付宝密钥工具产出的 `应用私钥PKCS8.txt`
   或 `应用私钥RSA2048.txt`（**两份文件任选其一即可，不必分辨**），
   `Ctrl+A` → `Ctrl+C` 全选复制，回到管理后台 `Ctrl+V` 粘贴进输入框。
6. 也可以直接点击输入框右上角的「**上传 .pem**」按钮选择文件，系统会自动读取内容。
7. 点击下方「**保存**」，再点击「**测试**」按钮：
   - 测试通过会显示「测试通过：联通性 OK」（实际文案以系统返回为准）
   - 测试失败会按错因给出友好文案，按提示处理即可

### 场景二：轮换密钥

支付宝商户出于安全考虑会定期更新 RSA2048 密钥对。轮换时操作完全相同：

1. 在支付宝开放平台密钥工具重新生成新的 RSA2048 密钥对
2. 把新生成的 **应用公钥** 内容上传到支付宝商户后台
3. 在管理后台「支付配置 → 支付宝 H5 → 配置」抽屉中，
   把新的 `应用私钥PKCS8.txt` **或** `应用私钥RSA2048.txt` 内容粘贴到「应用私钥」框
4. 保存 → 测试 → 通过即完成密钥轮换

### 场景三：误粘了「公钥」内容

如果不小心把「应用公钥」或「支付宝公钥」内容粘贴到了「应用私钥」框，
系统会立刻给出针对性提示：

> 您粘贴的内容看起来是「公钥」而不是「私钥」。请检查文件是否为应用私钥（PKCS8.txt 或 RSA2048.txt）。

按提示重新选择正确的私钥文件即可。

### 场景四：粘贴时不小心带了 BEGIN/END 头尾

不再是问题。修复后：
- 含 `-----BEGIN PRIVATE KEY-----` / `-----END PRIVATE KEY-----`（PKCS#8）→ 自动识别
- 含 `-----BEGIN RSA PRIVATE KEY-----` / `-----END RSA PRIVATE KEY-----`（PKCS#1）→ 自动识别
- 完全不含头尾的中间 Base64 → 自动识别

---

## 注意事项

1. **私钥安全**：「应用私钥」是商户绕过支付宝身份核验的关键凭据，
   请勿截图/粘贴到群聊/邮件等不受控渠道。系统已在所有日志中**屏蔽**私钥原文，
   只允许打印「长度 / 是否含 BEGIN 等元信息」。
2. **必须是 RSA 2048 位**：支付宝目前仅支持 RSA2048 算法的密钥对。
   如果误用 RSA1024 或非 RSA 算法生成的密钥，仍会被拒绝并给出明确提示。
3. **粘贴时不要截断**：如果从微信 / 企业微信 / 浏览器复制，请先确认整段
   内容完整。系统会自动剔除 BOM / 零宽空格 / 全角空格等不可见字符，
   但**不会**自行补全被截断的内容。
4. **「测试」按钮**：本次修复后，「测试」按钮会真实地向支付宝网关发起一次
   `alipay.trade.query` 调用（用一个绝不可能存在的订单号），从而验证
   网关连通性 + 签名链路是否正常。如果显示网络不通，请检查服务器出网/防火墙。
5. **接入模式不要乱切**：「公钥模式」与「证书模式」对应的支付宝商户后台
   配置完全不同，请确认贵公司在支付宝商户后台的实际接入模式后再选择。
6. **首次保存空值会被拒绝**：对于必填的敏感字段（如应用私钥），
   首次创建时若提交空值，会被 422 拒绝并给出「敏感字段 XX 首次创建时不能为空」提示，
   避免出现"看似保存成功，但其实库里是空"的迷惑性场景。

---

## 常见问题（FAQ）

**Q1：我之前已经粘了 `PKCS8.txt` 内容并保存成功，现在还要重新粘贴吗？**  
不需要。本次修复**完全向后兼容**，对 PKCS#8 用户体验毫无变化。
原本能用的私钥继续可用，原本能保存的配置继续有效。

**Q2：粘了 `RSA2048.txt` 内容会和粘了 `PKCS8.txt` 在数据库里存的形态不一样吗？**  
不会。两种粘贴方式落库前都会被**统一标准化为 PKCS#8 PEM 字符串**，
再 AES-256-GCM 加密入库。下游支付 SDK 拿到的永远是同一种规范化形态。

**Q3：如果我手上只有完整的 PEM 文件（含 `-----BEGIN ... PRIVATE KEY-----`
和 `-----END ... PRIVATE KEY-----` 头尾），可以直接粘贴吗？**  
可以。本次修复后，**含/不含头尾两种粘贴方式都支持**，PKCS#8 / PKCS#1
两种格式也都支持。系统会自动识别。

**Q4：我看到原来的报错文案里写「请用 PKCS8.txt 不是 RSA2048.txt」，
现在还会出现这条提示吗？**  
不会。这条带歧义的二次引导文案已彻底删除。修复后只会在
**确实无法识别**的情况下提示「请确认您粘贴的是支付宝密钥工具生成的
『应用私钥』内容（PKCS8.txt 或 RSA2048.txt 任一即可）」。

**Q5：本次修复影响其他支付通道吗？**  
不影响。微信小程序支付、微信 APP 支付、支付宝 APP 支付走各自的支付通道，
只有支付宝 H5 / 支付宝 APP 两个共享同一份私钥校验逻辑的通道受益。
所有相关测试用例（含 PKCS#8/PKCS#1、含/不含头尾、不可见字符、公钥误粘、乱码、截断、空值）
**34 个用例在测试服务器容器内全部 PASS**，老用例完全向后兼容。

**Q6：我可以通过哪种方式上传私钥？**  
两种方式都支持：
- **粘贴文本**：把 .txt 文件内容直接 Ctrl+V 进输入框
- **上传 .pem 文件**：点击输入框右上角的「上传 .pem」按钮选择文件

---

## 验收记录

- ✅ **测试服务器容器内 pytest**：34 个用例全部 PASS（覆盖 6 形态 PASS + 4 类异常分支）
- ✅ **后端 health endpoint**：HTTP 200
- ✅ **修复涉及文件**：
  - `backend/app/utils/rsa_key.py`（核心归一化逻辑）
  - `backend/app/api/payment_config.py`（保存与测试接口）
  - `admin-web/src/app/(admin)/payment-config/page.tsx`（输入框引导文案）
  - `backend/tests/test_alipay_private_key_format.py`（27 个核心用例）
  - `backend/tests/test_payment_config_alipay_save_validation.py`（保存校验）
  - `backend/tests/test_payment_config_test_connection_error_message.py`（测试连接错误文案）

---

## 访问链接

以下是当前项目的体验链接，点击即可打开：

> ⚠️ 所有链接均使用宿主机 Nginx 代理端口（80/443），请勿使用 Docker 容器内部端口。

| 服务 | 链接 | 说明 |
|------|------|------|
| 管理后台（admin-web） | [https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/](https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/admin/) | 登录后进入「支付配置」页面体验本次修复 |
| H5 用户端 | [https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/](https://newbb.test.bangbangvip.com/autodev/6b099ed3-7175-4a78-91f4-44570c84ed27/) | 终端用户的下单与支付入口 |

---

*本手册由 develop-all 流程自动生成，最终交付物。*
