# AI 客户端样式调整 — 需求沟通记录

## 时间
2026-05-23

## 需求概要
用户要求对 AI 客户端（ai-home 为主页的 H5 端）进行全量样式调整，按设计稿（方案A · 晴空诊室 — 全集核对版 23 屏）对齐。

## 已确认的关键决策

### 调整范围
- 全量调整——23 屏（29 态）全部更新

### 调整性质
- 以纯样式统一为主（颜色、间距、圆角、渐变、字体等视觉 token）
- 同时补齐 12 项设计稿中有但代码中缺失/不完整的组件

### 12 项需补齐组件
1. 欢迎区 + 健康贴士条（屏 ①④）
2. 功能宫格 → 维持现有胶囊条形态，只调色
3. 纯空对话页双色 SVG 推荐卡（屏 ④）→ 维持胶囊形态调色
4. 语音输入面板（屏 ⑥）→ 已有语音功能，UI 需改造对齐设计稿
5. 快捷操作 + 面板 4×2 网格浮层（屏 ⑫）→ 功能已有但分散，需整合为统一面板
6. 历史会话进入态提示条 + 回到当前浮钮（屏 ⑬）
7. 中断态提示 + 继续/重新按钮（屏 ⑭）
8. 隐私确认弹层（屏 ⑯）→ 只做 UI，触发逻辑后续定
9. 离线/弱网骨架屏态（屏 ⑰）
10. 报告详情页（屏 ㉗）
11. 医生详情页（屏 ㉘）→ 先按设计稿做 UI，接口后续对
12. 预约时间选择页（屏 ㉙）→ 先按设计稿做 UI，接口后续对

### 验收标准
- 像素级对齐，偏差不超过 2px

### 交付方式
- 一次性全部完成再交付（不分批）

### HTML 设计图要求
- 全量出图（29 屏 + 12 项补齐组件）
- 高保真——像素级还原
- 整合为一份 HTML 文件（同一页滚动查看）
- 新增组件双重展示：融入对应屏幕 + 末尾汇总清单

### 代码现状分析（快捷操作面板）
- 8 项功能在代码中全部存在，但以横向胶囊条 + 分散 ActionSheet 形式
- 需要新增统一的 4×2 底部弹出面板组件

## 已输出文件
- 全量高保真 HTML 设计图（.chat_output/14bd0e4d/）
- 等用户确认后将输出最终 PRD 文档
