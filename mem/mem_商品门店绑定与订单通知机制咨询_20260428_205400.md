# 商品门店绑定与订单通知机制咨询（续）

## 时间
2026-04-28 20:58

## 用户原始发言
用户选择：
1. B（了解现有机制是否完善）+ C（规划新的门店订单/预约提醒功能）+ 补充：商品管理中没看到绑定门店的地方，要求检查代码
2. D（全部关心：即时推送 + 门店端订单列表）

## 代码检查结果
- **后端已完整实现**：`ProductStore` 模型（product_stores 多对多表）、`ProductCreate`/`ProductUpdate` schema 含 `store_ids` 字段、创建/编辑/删除接口均已处理门店关联
- **管理后台前端缺失**：商品编辑弹窗（`admin-web/src/app/(admin)/product-system/products/page.tsx`）中**没有门店选择 UI 组件**
- 对比：商户账号页面（`admin-web/src/app/(admin)/merchant/accounts/page.tsx`）有门店多选组件，但商品管理页面遗漏了

## 现有通知机制
- `merchant_notifications` 表可推送消息到商家端
- 核销记录含 store_id + verified_by_user_id
- 订单本身不直接记录 store_id，核销时才关联门店

## 当前状态
已向用户确认代码检查结果（前端缺失门店选择组件），正在进一步引导用户细化需求：
1. 商品绑定门店的前端补全程度
2. 通知渠道选择
3. 门店端查看订单的方式
