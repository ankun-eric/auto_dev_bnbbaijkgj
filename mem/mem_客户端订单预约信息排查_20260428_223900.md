# 客户端订单预约信息排查

## 时间
2026-04-28 22:39

## 用户发言
客户端订单里看不到预约信息，请查下代码是否有了。

## 排查结论

### 后端 API
- `OrderItemResponse` 已包含 `appointment_data` 和 `appointment_time` 字段，数据完整。

### 各端订单详情预约信息展示情况

| 端 | 状态 | 说明 |
|---|---|---|
| Flutter APP | ✅ 已实现 | `unified_order_detail_screen.dart` 中有 `_buildAppointmentSection` 方法 |
| 管理后台 | ✅ 已实现 | `orders/page.tsx` 中有"预约信息"Card |
| 微信小程序 | ⚠️ 有问题 | wxml 模板有预约信息区块，但依赖 `order.appointment_date`（订单级字段），而后端返回的是 `items[].appointment_time`（子项级字段），JS 中未做映射，实际显示不出来 |
| H5 Web | ❌ 完全缺失 | 订单详情页无任何预约相关代码，OrderItem 接口定义中也未声明预约字段 |

### 需要修复的端
1. H5 Web：需要在订单详情页新增预约信息展示模块
2. 微信小程序：需要在 JS 中从 items 提取预约信息映射到模板所需字段

## 涉及文件
- `h5-web/src/app/unified-order/[id]/page.tsx` — 缺失预约信息
- `miniprogram/pages/unified-order-detail/index.js` — 缺少预约数据映射
- `miniprogram/pages/unified-order-detail/index.wxml` — 模板存在但数据未对接
- `flutter_app/lib/screens/order/unified_order_detail_screen.dart` — 已正常
- `admin-web/src/app/(admin)/product-system/orders/page.tsx` — 已正常
- `backend/app/schemas/unified_orders.py` — API 数据已包含预约字段
