# 记忆：AI健康咨询对话页输入栏改造 + 拍照识药按钮

## 日期
2026-04-16

## 用户原始需求
- 问题14：用户指的是跳入AI健康咨询对话页（/chat/[sessionId]）后的页面，而非AI Tab首页
- 问题15：选A
- 问题16：选C，输入栏布局改成跟健康自查完全一致：「咨询人头像 → 语音/键盘切换 → 输入框 → 发送」；在输入栏上方加「图标+拍照识药」按钮

## 执行步骤与决策
1. 分析了 `h5-web/src/app/chat/[sessionId]/page.tsx`（AI健康咨询对话页）的现有输入栏布局
2. 参照 `h5-web/src/app/symptom/page.tsx`（健康自查页面）的输入栏布局风格
3. 参照 `h5-web/src/app/drug/page.tsx`（拍照识药页面）的相机图标设计
4. 调整输入栏元素顺序：将语音/键盘切换按钮从输入框右侧移到输入框左侧（头像之后）
5. 将发送按钮从输入框内部提取为独立按钮放在最右侧
6. 在输入栏上方新增「拍照识药」快捷按钮（相机图标+文字，点击跳转/drug页面）

## 涉及的关键文件
- `h5-web/src/app/chat/[sessionId]/page.tsx` — 主要修改文件，输入栏布局改造+拍照识药按钮
- `h5-web/src/app/symptom/page.tsx` — 参考：健康自查页面布局
- `h5-web/src/app/drug/page.tsx` — 参考：拍照识药功能页面

## 最终结果
- 输入栏布局顺序改为：咨询人头像 → 语音/键盘切换 → 输入框 → 发送按钮
- 输入栏上方新增绿色浅底圆角胶囊样式的「拍照识药」快捷入口
- 编译通过，无错误
