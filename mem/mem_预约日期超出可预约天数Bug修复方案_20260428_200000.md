# 预约日期选择超出提前可预约天数 Bug — 修复方案文档已输出

## 时间
2026-04-28 20:00

## 用户决策汇总

### 问题 1：影响范围
- 选择 A：H5 + 小程序 + Flutter APP 三端都需要修复

### 问题 2：处理方式
- 选择 A + C 组合：超出范围日期**置灰不可选** + 日期选择器上方显示"最远可预约至 X月X日"提示文字

### 问题 3：天数计算方式
- 选择 A（主流做法）：**包含今天**，设置 7 天 = 今天 + 后面 6 天 = 共 7 个自然日可选

## Bug 根因回顾
- 后端 API 已正确返回 `advance_days` 字段
- H5 端 `DatePicker` 只设了 `min` 没设 `max`，`ProductInfo` interface 未定义 `advance_days`
- 小程序端 `picker` 只设了 `start` 没设 `end`，未读取 `advance_days`
- Flutter APP 端预计存在相同问题

## 修复方案文档
- 已输出 Bug 修复方案文档：`docs/BUG_FIX_预约日期选择超出提前可预约天数_20260428_200000.md`

## 当前状态
Bug 修复方案文档已输出完毕，等待用户在新会话中进行实际修复。
